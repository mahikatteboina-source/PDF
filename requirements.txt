# Lightweight dependencies for Netlify build

streamlit==1.37.1
pymupdf==1.24.9
pillow==10.4.0

# Transformers (uses tokenizers, make sure Python is 3.11 so wheels are available)
transformers==4.44.2

# CPU‑only PyTorch stack (much smaller than CUDA builds)
torch==2.4.1+cpu
torchvision==0.19.1+cpu
torchaudio==2.4.1+cpu
